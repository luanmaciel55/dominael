import{tickIncome,rank}from'./reinos-economy.js';import{settle}from'./reinos-caravans.js';import{runBots}from'./reinos-bots.js';export function advance(s,regs){tickIncome(s,regs);settle(s);runBots(s,regs);if(!s.finished&&Date.now()>=s.endsAt){s.finished=true;let w=rank(s)[0];s.winner=w.name;s.news.unshift({at:Date.now(),text:`🏆 ${w.name} venceu a temporada com ${w.score} pontos!`})}return s}export function remaining(s){let ms=Math.max(0,s.endsAt-Date.now()),d=Math.floor(ms/86400000),h=Math.floor(ms%86400000/3600000),m=Math.floor(ms%3600000/60000);return s.finished?'Temporada encerrada':`${d}d ${h}h ${m}m restantes`}