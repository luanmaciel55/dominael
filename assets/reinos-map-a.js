import{neigh}from'./reinos-world-b.js';import{colors}from'./reinos-world-a.js';export function drawMap(svg,s,regs,onPick){let out='';for(let r of regs)for(let n of neigh(r.id))if(n>r.id){let d=regs[n];out+=`<line class="rm-road" x1="${r.x}" y1="${r.y}" x2="${d.x}" y2="${d.y}"/>`}for(let r of regs){let owner=s.owners[r.id],fill=owner==null?'#d8c98d':colors[owner],sel=s.selected===r.id?' stroke="#fff" stroke-width="6"':' stroke="#24382a" stroke-width="3"';out+=`<g data-rid="${r.id}" class="rm-region"><circle cx="${r.x}" cy="${r.y}" r="43" fill="${fill}"${sel}/><text class="rm-label" x="${r.x}" y="${r.y-52}" text-anchor="middle">${r.name}</text><text x="${r.x}" y="${r.y+6}" text-anchor="middle" font-size="22">${owner==null?'🏞️':r.type==='port'?'⚓':'🏘️'}</text></g>`}svg.innerHTML=out;svg.querySelectorAll('[data-rid]').forEach(x=>x.addEventListener('click',()=>onPick(+x.dataset.rid)))}