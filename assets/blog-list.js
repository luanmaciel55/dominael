const C=window.DOMINAEL_CONFIG||{},$=q=>document.querySelector(q),esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function author(c){return c?`<a class="post-author-mini" href="${esc(c.href)}">${c.photo_url?`<img src="${esc(c.photo_url)}" alt="${esc(c.name)}">`:'<span class="post-author-placeholder">✦</span>'}<span><small>Autor / idealizador</small><b>${esc(c.name)}</b></span></a>`:''}async function load(){const q=new URLSearchParams(location.search),page=Math.max(1,Number(q.get('page')||1)),cat=q.get('category')||'',r=await fetch(`${C.functionsBase}/blog-public?page=${page}${cat?`&category=${encodeURIComponent(cat)}`:''}`,{cache:'no-store'}),d=await r.json();if(!r.ok)throw Error(d.error||'Erro');$('#blogCategories').innerHTML=`<a class="${!cat?'active':''}" href="/postagens.html">Todos</a>`+(d.categories||[]).map(c=>`<a class="${cat===c.slug?'active':''}" href="/postagens.html?category=${encodeURIComponent(c.slug)}">${esc(c.name)}</a>`).join('');$('#postList').innerHTML=(d.posts||[]).map(p=>`<article class="post-card"><div>${p.cover_url?`<img src="${p.cover_url}" alt="${esc(p.title)}">`:'<div style="height:150px;border-radius:16px;background:#eaf0f4"></div>'}</div><div><div class="post-meta">${p.category?esc(p.category.name):'Postagem'}${p.show_date&&p.published_at?' · '+new Date(p.published_at).toLocaleDateString('pt-BR'):''}</div><h2>${esc(p.title)}</h2>${p.subtitle?`<strong>${esc(p.subtitle)}</strong>`:''}${author(p.creator)}<p>${esc(p.excerpt||'Leia o conteúdo completo desta postagem.')}</p><a class="btn btn-primary" href="/postagem.html?slug=${encodeURIComponent(p.slug)}">Ver conteúdo</a></div></article>`).join('')||'<div class="notice">Nenhuma postagem publicada nesta categoria.</div>';let nav='';for(let i=1;i<=d.total_pages;i++)nav+=`<a class="${i===d.page?'active':''}" href="/postagens.html?page=${i}${cat?`&category=${encodeURIComponent(cat)}`:''}">${i}</a>`;$('#blogPagination').innerHTML=nav}document.addEventListener('DOMContentLoaded',()=>load().catch(e=>{$('#postList').innerHTML=`<div class="notice error">${esc(e.message)}</div>`}));