import './admin-preflight.js?v=20260914-1';
import './admin-pro.js';
import './admin-course-extra.js';
import './contribution-admin.js?v=20260913-2';
import './course-admin.js';
import './creator-profile-admin.js';
import './blog-author-admin.js';
import './site-menu-admin.js';
import './admin-generators.js';
import './admin-categories.js';
import './admin-vale-cards.js?v=20260914-1';
if(!document.querySelector('link[href="/assets/courses.css"]')){const l=document.createElement('link');l.rel='stylesheet';l.href='/assets/courses.css';document.head.appendChild(l)}
if(!document.querySelector('link[href="/assets/creator-profile-v2.css"]')){const l=document.createElement('link');l.rel='stylesheet';l.href='/assets/creator-profile-v2.css';document.head.appendChild(l)}
import{createClient}from'https://esm.sh/@supabase/supabase-js@2.116.0';
const C=window.DOMINAEL_CONFIG||{},s=createClient(C.supabaseUrl,C.supabasePublishableKey),$=q=>document.querySelector(q),esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),money=v=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format((v||0)/100),dt=v=>v?new Intl.DateTimeFormat('pt-BR',{dateStyle:'short',timeStyle:'medium'}).format(new Date(v)):'—';
async function api(name,opt={}){const{data}=await s.auth.getSession();const r=await fetch(`${C.functionsBase}/${name}`,{...opt,headers:{'Content-Type':'application/json','Authorization':`Bearer ${data.session?.access_token||''}`}}),d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Erro');return d}
let reviews=[],products=[];
function renderReviews(){const sel=$('#reviewProduct'),box=$('#reviewRows');if(sel)sel.innerHTML=products.map(p=>`<option value="${p.id}">${esc(p.name)}</option>`).join('');if(!box)return;box.innerHTML=reviews.length?reviews.map(r=>{const p=products.find(x=>x.id===r.product_id);return `<div class="admin-review"><div><b>${esc(p?.name||'Produto')}</b><div class="review-stars">${'★'.repeat(r.rating)}${'☆'.repeat(5-r.rating)} <small>${r.rating}/5</small></div><p>${esc(r.comment)}</p><small>${dt(r.created_at)}</small></div><button class="btn btn-light" data-review-delete="${r.id}">Apagar</button></div>`}).join(''):'<div class="notice">Nenhuma avaliação cadastrada.</div>';document.querySelectorAll('[data-review-delete]').forEach(b=>b.onclick=async()=>{if(!confirm('Apagar esta avaliação?'))return;await api('admin-products',{method:'POST',body:JSON.stringify({resource:'review',action:'delete',id:b.dataset.reviewDelete})});await loadReviews()})}
async function loadReviews(){const d=await api('admin-products');reviews=d.reviews||[];products=d.products||[];renderReviews()}
function geo(x){return [x.city,x.region,x.country].filter(Boolean).join(' / ')||'Localização não disponível'}
async function enhanceAnalytics(){const host=$('#analyticsCards');if(!host)return;try{const d=await api('admin-analytics'),active=document.querySelector('.analytics-period.active')?.dataset.period||'day',x=d.periods?.[active];if(!x)return;host.querySelectorAll('.analytics-extra').forEach(n=>n.remove());const wrap=document.createElement('div');wrap.className='best-sellers-card analytics-extra';wrap.innerHTML=`<h3>Acessos usando senha do produto</h3>${x.password_accesses?.length?x.password_accesses.map(a=>`<div class="best-row"><b>${esc(a.name)}</b><span>${a.count} vez(es)</span></div>`).join(''):'<div class="mini">Nenhum acesso por senha registrado neste período.</div>'}<h3 style="margin-top:24px">Vendas — data, horário e local</h3>${x.recent_sales?.length?x.recent_sales.map(v=>`<div class="analytics-detail"><b>${esc(v.name)}</b><span>${money(v.amount_cents)} · ${dt(v.date)}</span><small>${esc(geo(v))}</small></div>`).join(''):'<div class="mini">Nenhuma venda neste período.</div>'}<h3 style="margin-top:24px">Visitas — data, horário e local</h3>${x.recent_visits?.length?x.recent_visits.map(v=>`<div class="analytics-detail"><span>${dt(v.date)}</span><small>${esc(geo(v))}</small></div>`).join(''):'<div class="mini">Nenhuma visita neste período.</div>'}`;host.appendChild(wrap)}catch(e){console.error(e)}}
document.addEventListener('DOMContentLoaded',()=>{document.querySelector('[data-tab="reviewsPanel"]')?.addEventListener('click',()=>setTimeout(loadReviews,50));document.querySelector('[data-tab="analyticsPanel"]')?.addEventListener('click',()=>setTimeout(enhanceAnalytics,500));document.querySelectorAll('.analytics-period').forEach(b=>b.addEventListener('click',()=>setTimeout(enhanceAnalytics,100)));$('#refreshAnalytics')?.addEventListener('click',()=>setTimeout(enhanceAnalytics,600));$('#reviewForm')?.addEventListener('submit',async e=>{e.preventDefault();const msg=$('#reviewMsg');try{msg.innerHTML='<div class="notice">Salvando...</div>';await api('admin-products',{method:'POST',body:JSON.stringify({resource:'review',product_id:$('#reviewProduct').value,rating:Number($('#reviewRating').value),comment:$('#reviewComment').value.trim()})});e.target.reset();msg.innerHTML='<div class="notice success">Avaliação adicionada.</div>';await loadReviews()}catch(x){msg.innerHTML=`<div class="notice error">${esc(x.message)}</div>`}})})