import{createClient}from'https://esm.sh/@supabase/supabase-js@2.116.0';const C=window.DOMINAEL_CONFIG||{},client=createClient(C.supabaseUrl,C.supabasePublishableKey),$=q=>document.querySelector(q),esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),money=v=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format((v||0)/100);let rows=[];async function token(){const{data}=await client.auth.getSession();return data.session?.access_token||''}async function api(opt={}){const r=await fetch(`${C.functionsBase}/admin-users`,{...opt,headers:{'Content-Type':'application/json','Authorization':`Bearer ${await token()}`}}),d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Erro');return d}function idle(v){if(!v)return'Nunca registrado';const ms=Date.now()-new Date(v).getTime(),m=Math.max(0,Math.floor(ms/60000));if(m<1)return'Agora';if(m<60)return`${m} min`;const h=Math.floor(m/60);if(h<24)return`${h} h`;const d=Math.floor(h/24);if(d<30)return`${d} dia(s)`;return`${Math.floor(d/30)} mês(es)`}function render(list=rows){const q=($('#usersSearch')?.value||'').trim().toLowerCase();const arr=list.filter(u=>!q||String(u.username||'').toLowerCase().includes(q)||String(u.email||'').toLowerCase().includes(q));$('#usersRows').innerHTML=arr.map(u=>`<article class="user-admin-card"><div class="user-admin-main"><div><h3>${esc(u.username)}</h3><p>${esc(u.email||'Sem e-mail')}</p><small>Cadastrado: ${new Date(u.created_at).toLocaleString('pt-BR')} · Inativo há: <b>${idle(u.last_seen_at)}</b></small></div><div class="user-spend"><span>Total gasto</span><strong>${money(u.total_spent_cents)}</strong><small>${u.purchase_count} compra(s)</small></div></div><details><summary>Ver onde gastou</summary><div class="user-purchases">${u.purchases?.length?u.purchases.map(p=>`<div><b>${esc(p.area)}</b><span>${esc(p.item)}</span><strong>${money(p.amount_cents)}</strong><small>${new Date(p.date).toLocaleString('pt-BR')}</small></div>`).join(''):'<p class="mini">Nenhuma compra paga vinculada a esta conta.</p>'}</div></details><button class="btn btn-danger" data-delete-user="${u.id}">Excluir conta definitivamente</button></article>`).join('')||'<div class="notice">Nenhum usuário encontrado.</div>';document.querySelectorAll('[data-delete-user]').forEach(b=>b.onclick=()=>del(b.dataset.deleteUser))}async function load(){const box=$('#usersRows');if(box)box.innerHTML='<div class="loading">Carregando usuários...</div>';try{const d=await api();rows=d.users||[];$('#usersSummary').innerHTML=`<div class="metric-card"><span>Usuários cadastrados</span><strong>${d.total_users||0}</strong></div><div class="metric-card"><span>Total gasto por cadastrados</span><strong>${money(d.total_spent_cents||0)}</strong></div><div class="metric-card"><span>Maior comprador</span><strong>${rows[0]?esc(rows[0].username):'—'}</strong><small>${rows[0]?money(rows[0].total_spent_cents):''}</small></div>`;render()}catch(e){box.innerHTML=`<div class="notice error">${esc(e.message)}</div>`}}async function del(id){const u=rows.find(x=>x.id===id);if(!u)return;if(!confirm(`Excluir definitivamente a conta ${u.username}?\n\nIsso apaga progresso, compras vinculadas, cursos, jogos e sessões dessa conta.`))return;if(!confirm('Esta ação não pode ser desfeita. Confirmar exclusão definitiva?'))return;try{await api({method:'POST',body:JSON.stringify({action:'delete',user_id:id})});rows=rows.filter(x=>x.id!==id);render();alert('Conta excluída definitivamente.')}catch(e){alert(e.message)}}document.addEventListener('DOMContentLoaded',()=>{$('#usersSearch')?.addEventListener('input',()=>render());$('#refreshUsers')?.addEventListener('click',load);document.querySelector('[data-tab="usersPanel"]')?.addEventListener('click',()=>load())});