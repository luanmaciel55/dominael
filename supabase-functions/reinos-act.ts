import{claim,build}from'./reinos-actions-a.ts';import{produce,caravan}from'./reinos-actions-b.ts';export function act(st:any,seat:number,b:any){if(st.finished)throw Error('Temporada encerrada.');if(b.action==='claim')return claim(st,seat,+b.region_id);if(b.action==='build')return build(st,seat,+b.region_id,String(b.building||''));if(b.action==='produce')return produce(st,seat);if(b.action==='caravan')return caravan(st,seat,+b.from,+b.to,+b.qty);throw Error('Ação inválida.')}